import { Background } from '@/components/Modal/Modal.styles'
import React from 'react'

const ModalBackground = ({ children }) => {
  return <Background>{children}</Background>
}

export default ModalBackground
